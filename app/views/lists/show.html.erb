<h1>List Name: <%=@list.name %> </h1>
<% @list.bookmarks.each do |bookmark| %>
  <li><%= bookmark.movie.title%> <%= link_to "remove", bookmark_path(bookmark), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%></li>
      <%= bookmark.comment %>
<% end %>


<p><%= link_to "Add bookmark", new_list_bookmark_path(@list) %></p>
